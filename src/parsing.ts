const pathName = [
  "slug",
  // "gameSlug",
  // "gameId",
  // "play",
  // "superSlug",
  "held9i6l_3",
];

const [_slug, _gameSlug, gameId, _, _superSlug, publicationId] = pathName;

console.log(_slug, _gameSlug, gameId, _, _superSlug, publicationId);
